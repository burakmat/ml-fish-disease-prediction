services:
  backend:
    build: ./backend
    ports:
      - "8000:8000"
    volumes:
      - ./backend/ml_model:/app/ml_model
  frontend:
    build: ./frontend
    ports:
      - "80:5173"
    environment:
      - VITE_BACKEND_URL=http://localhost:8000/
    volumes:
      - ./frontend/src:/app/src